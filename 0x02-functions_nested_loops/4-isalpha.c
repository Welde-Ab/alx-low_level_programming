#include "main.h"
#include <ctype.h>
/**
 * _isalpha - check the code.
 * @c - character to be checked
 * Return: 1 - if alpha or 0 - if not alpha
 */
int _isalpha(int c)
{
int stat = isalpha(c);
return (stat);
}
